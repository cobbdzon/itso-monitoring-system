<!DOCTYPE html>
<html>

<head>
    <title>
        CLA
    </title>
    <link rel='stylesheet' href='/css/admin.css' />
</head>

<body>
    <%- include('partials/nav', {navLocation: navLocation}); %>
    <main>
        <section id="profile">
            <header>
                Welcome<br> 
                <b> <%= username %> </b>
            </header>
            <span>
                to the admin panel.
            </span>
            <div id="buttons">
                <form action="/api/logout" method="post">
                    <input type="submit" value="Logout">
                </form>
            </div>
        </section>
        <section id="users">
            <li class="indicator">
                <div>User</div>
                <div>Status</div>
                <div>Last Log</div>
            </li>
            <% users.forEach(user => { %>
                <li class="user">
                    <div class="username">
                        <%= user.username %> 
                    </div>
                    <div class="status">
                        <% if (timedInUsers[user.id]) { %>
                            <div class="in">
                                Timed In
                            </div>
                        <% } else { %>
                            <div class="out">
                                Timed Out
                            </div>
                        <% } %>
                    </div>
                    <div class="last-log">
                        <% const logDate = new Date(user.history[0].time) %>
                        <%= `${logDate.toDateString()}, ${logDate.toLocaleTimeString()}` %>
                    </div>
                    <div class="timelog">
                        <a href="/profile/<%= user.id %> ">View Profile</a>
                    </div>
                </li>
            <% }) %>
            <% if (Object.keys(users).length <= 0) { %>
                <div class="empty">
                    <span>It looks quite empty here...</span>
                </div>
            <% } %> 
        </section>
    </main>
</body>

</html>